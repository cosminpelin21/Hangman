<Window x:Class="Hangman.Views.NewUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="New User" Height="300" Width="450" 
        WindowStartupLocation="CenterOwner" Background="#2D2D30">

    <Window.Resources>
        <!-- Stil Butoane -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="0,5,0,2"/>
        </Style>
    </Window.Resources>

    <StackPanel Margin="30" VerticalAlignment="Center">
        <TextBlock Text="User name:" FontWeight="Bold" Foreground="White"/>
        <TextBox x:Name="UserNameTextBox" Margin="0,5,0,15" Padding="5" 
                 Background="#3E3E42" Foreground="White" BorderThickness="0"/>

        <TextBlock Text="Image path:"/>
        <StackPanel Orientation="Horizontal" Margin="0,5,0,20">
            <TextBox x:Name="ImagePathTextBox" Width="280" IsReadOnly="True" 
                     Padding="5" Background="#3E3E42" Foreground="White" BorderThickness="0"/>
            <Button Content="Select..." Click="PickImage_Click" Width="80" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Click="Save_Click" Width="100" Height="30" Margin="0,0,10,0" Background="#28A745"/>
            <Button Content="Cancel" Click="Cancel_Click" Width="100" Height="30" Background="#6C757D"/>
        </StackPanel>
    </StackPanel>
</Window>