<Window x:Class="Hangman.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Hangman"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to Hangman! Enjoy this game!" 
                   FontSize="20" HorizontalAlignment="Center" Margin="10"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox ItemsSource="{Binding Users}" 
                     SelectedItem="{Binding SelectedUser}" 
                     Margin="20" DisplayMemberPath="Name"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <Image Source="{Binding SelectedUser.ImagePath}" Height="150" Width="150"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10">
                    <Button Content="«" Width="40" Margin="5" Command="{Binding PreviousUserCommand}"/>
                    <Button Content="»" Width="40" Margin="5" Command="{Binding NextUserCommand}"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="20">
            <Button Content="New User" Command="{Binding AddUserCommand}" Width="100" Margin="10"/>
            <Button Content="Delete User" Command="{Binding DeleteUserCommand}" Width="100" Margin="10"/>
            <Button Content="Play" Command="{Binding PlayCommand}" Width="100" Margin="10"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="10"/>
        </StackPanel>
    </Grid>
</Window>
