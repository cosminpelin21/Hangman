<Window x:Class="Hangman.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Hangman - Login" Height="450" Width="800" 
        WindowStartupLocation="CenterScreen" Background="#2D2D30">

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="Background" Value="#007ACC"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1C8FDC"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Welcome to Hangman!" 
                   FontSize="28" Foreground="White" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <ListBox ItemsSource="{Binding Users}" 
                     SelectedItem="{Binding SelectedUser}" 
                     Margin="0,0,20,0" DisplayMemberPath="Name" 
                     Background="#3E3E42" Foreground="White" BorderThickness="0"
                     FontSize="16" Padding="10"/>

            <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                <Ellipse Width="200" Height="200" 
                        Stretch="UniformToFill" 
                        StrokeThickness="0">
                    <Ellipse.Fill>
                        <ImageBrush ImageSource="{Binding SelectedUser.ImagePath}" 
                        Stretch="UniformToFill" 
                        AlignmentX="Center" 
                        AlignmentY="Center"/>
                    </Ellipse.Fill>
                </Ellipse>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="«" Width="50" Height="30" Margin="5" Command="{Binding PreviousUserCommand}"/>
                    <Button Content="»" Width="50" Height="30" Margin="5" Command="{Binding NextUserCommand}"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button Content="New User" Command="{Binding AddUserCommand}" Width="100" Margin="5"/>
            <Button Content="Delete User" Command="{Binding DeleteUserCommand}" Width="100" Margin="5"/>
            <Button Content="Play" Command="{Binding PlayCommand}" Width="100" Margin="5" Background="#28A745"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="100" Margin="5" Background="#DC3545"/>
        </StackPanel>
    </Grid>
</Window>